# Incident report: Cardinality collapse

> Verdict: **FAIL** · Severity: **critical** · Drift detected: **true**

Critical incident: cardinality collapse on customer_id (303 -> 15).

## Run

- System: `data-quality-watchtower`
- Run id: `dqw-2026-08-15-cardinality_collapse`
- Fixture: `orders` (baseline 500 rows vs current 500 rows)
- Scenario: `cardinality_collapse` (A foreign key loses most of its unique values.)
- Generated at: 2026-08-15T06:19:13Z
- Checks run: 50 · Anomalies detected: 1 · Schema drifts: 0

## What the scan found

| Kind | Column | Detail |
|---|---|---|
| cardinality_collapse | `customer_id` | baseline_unique_count=303, candidate_unique_count=15 |

## Why it matters

- Column `customer_id` distinct values collapsed from 303 to 15. A high-cardinality key is now nearly constant, so joins fan out or collapse while the query still runs.

## Reproduce

```bash
git clone https://github.com/IgnazioDS/data-quality-watchtower.git
cd data-quality-watchtower && pip install -e .
python -m data_quality_watchtower.incident_runner
cat api/_incident_latest.json
```

All fixtures are synthetic, seeded, and committed in-repo. The scenario is selected by date, so this run reproduces exactly when re-run for the same day.
