# Incident report: Schema add

> Verdict: **PASS** · Severity: **warning** · Drift detected: **true**

Warning incident: new columns detected: discount_code.

## Run

- System: `data-quality-watchtower`
- Run id: `dqw-2026-08-27-schema_add`
- Fixture: `orders` (baseline 500 rows vs current 500 rows)
- Scenario: `schema_add` (An upstream producer adds an unexpected column.)
- Generated at: 2026-08-27T17:03:17Z
- Checks run: 51 · Anomalies detected: 1 · Schema drifts: 1

## What the scan found

| Kind | Column | Detail |
|---|---|---|
| schema_add | `discount_code` | present_in=candidate, absent_in=baseline |

## Why it matters

- New column `discount_code` appeared that is absent from the baseline. Readers pinned to a fixed column set will ignore it or reject the row.

## Reproduce

```bash
git clone https://github.com/IgnazioDS/data-quality-watchtower.git
cd data-quality-watchtower && pip install -e .
python -m data_quality_watchtower.incident_runner
cat api/_incident_latest.json
```

All fixtures are synthetic, seeded, and committed in-repo. The scenario is selected by date, so this run reproduces exactly when re-run for the same day.
