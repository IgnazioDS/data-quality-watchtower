# Incident report: Value-distribution shift

> Verdict: **FAIL** · Severity: **warning** · Drift detected: **true**

Warning incident: numeric shift on revenue_usd (mean delta -0.826, outlier delta +0.024).

## Run

- System: `data-quality-watchtower`
- Run id: `dqw-2026-08-19-value_distribution_shift`
- Fixture: `orders` (baseline 500 rows vs current 500 rows)
- Scenario: `value_distribution_shift` (A billing column collapses toward zero.)
- Generated at: 2026-08-19T06:24:38Z
- Checks run: 50 · Anomalies detected: 1 · Schema drifts: 0

## What the scan found

| Kind | Column | Detail |
|---|---|---|
| distribution_shift | `revenue_usd` | baseline_mean=1651.381, candidate_mean=286.916, mean_delta_ratio=-0.826 |

## Why it matters

- Column `revenue_usd` mean moved from 1651.381 to 286.916 (ratio -0.826). The values shifted in a plausible but unverified direction.

## Reproduce

```bash
git clone https://github.com/IgnazioDS/data-quality-watchtower.git
cd data-quality-watchtower && pip install -e .
python -m data_quality_watchtower.incident_runner
cat api/_incident_latest.json
```

All fixtures are synthetic, seeded, and committed in-repo. The scenario is selected by date, so this run reproduces exactly when re-run for the same day.
