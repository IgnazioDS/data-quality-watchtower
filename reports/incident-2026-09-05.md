# Incident report: Type coercion

> Verdict: **FAIL** · Severity: **critical** · Drift detected: **true**

Critical incident: type changes: quantity (integer -> string).

## Run

- System: `data-quality-watchtower`
- Run id: `dqw-2026-09-05-type_coercion`
- Fixture: `orders` (baseline 500 rows vs current 500 rows)
- Scenario: `type_coercion` (A numeric column starts arriving as free text.)
- Generated at: 2026-09-05T09:37:37Z
- Checks run: 50 · Anomalies detected: 1 · Schema drifts: 1

## What the scan found

| Kind | Column | Detail |
|---|---|---|
| type_change | `quantity` | baseline_type=integer, candidate_type=string |

## Why it matters

- Column `quantity` changed type from integer to string. Numeric consumers will coerce or reject the values without warning.

## Reproduce

```bash
git clone https://github.com/IgnazioDS/data-quality-watchtower.git
cd data-quality-watchtower && pip install -e .
python -m data_quality_watchtower.incident_runner
cat api/_incident_latest.json
```

All fixtures are synthetic, seeded, and committed in-repo. The scenario is selected by date, so this run reproduces exactly when re-run for the same day.
