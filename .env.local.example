# This dashboard requires no environment variables to build or run.
# It is a public, static Next.js app with no server-side secrets.
# Future environment variables (if any) should use the NEXT_PUBLIC_ prefix for client-side access.
